import { IAction } from "../../../../datastructure/action/interfaces/iAction";

export const createMoveString = (to: number): string => {
    let retVal: string = " ";
    const prefix = "";

    return retVal;
};
